{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Every entry is something the frontend calls; see src-tauri/tests/capabilities.rs for what is refused and why.",
  "windows": [
    "main"
  ],
  "permissions": [
    "mailbox",
    "plugin-probe",
    "core:default",
    "core:event:default",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
